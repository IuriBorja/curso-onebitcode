let velocidade = 80;

while (velocidade > 0) {
    alert(`A velocidade é ${velocidade} km/h`);
    velocidade -= 20;
}

alert("O carro parou.");


/* OUTRO EXEMPLO */

let count = 1;

while (count <= 5) {
    alert(`Numero ${count}`);
    count++;
}